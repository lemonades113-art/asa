# [YOUR_NAME]

**[YOUR_PHONE]** | **[YOUR_EMAIL]** | **GitHub: [YOUR_GITHUB]**
求职意向：AI算法工程师 / 金融大模型算法工程师

---

## 教育背景

**[YOUR_UNIVERSITY]** | [硕士/本科] | [专业名称] | [20XX.09 – 20XX.06]

- GPA: [X.XX / 4.0] | 主修课程：机器学习、自然语言处理、数据结构与算法、概率论与统计
- 获奖情况：[如有填写]

---

## 项目经历

---

### ASA · A股多智能体投研推理引擎 | 核心开发者 | 2024.12 – 2026.02

Python / LangGraph / DeepSeek-v3 / BGE-Small-zh-v1.5 / FAISS / BM25 / Tushare Pro

构建面向A股投研场景的多Agent自动化系统，针对金融LLM三大共性难题（字段幻觉、推理轨迹偏移、多步数值不可信），构建从意图拆解→多接口数据获取→计算建模→研报产出的全链路闭环，覆盖跨表财务对比、杜邦分析及行业估值预警等场景。

**多步推理轨迹控制（Planning & Steering）**

针对多步财务分析中模型随中间空数据提前宣告完成的"逻辑早停"问题，以数据协议验证非空作为步骤推进的唯一门控，结合JSON结构化思维链强制模型遵循"意图识别→参数校验→逻辑建模"的执行路径；通过Pydantic约束路由决策输出消除文本解析歧义，配合最小权限矩阵对每个Agent的工具访问权进行角色隔离。

**Easy Pass@1 = 100%，Medium（多步财务建模）Pass@1 = 87.5%，avg_latency = 364s**

**属性级语义对齐与混合RAG检索（Schema Linking & Retrieval）**

针对Tushare 340+接口的字段幻觉问题（用户语"赚钱能力"与API字段`roe_ttm`语义相近但符号不等价，标准向量检索无法区分），将340篇文档重构为4032个单字段语义块，检索粒度降至字段级；查询侧以金融同义词表扩展至API字段名增强精确匹配，结合向量相似度（α=0.7）与BM25词频权重融合排序，辅以正则规则+结构化意图提取三路召回兜底。

**RAG Hit@3 = 93.1%，MRR@5 = 87.1%（101样本）；结构化输出解析失败率从~5%降至~1%**

**指令流与数据流分离的执行内核及四级故障归因（Execution & Fault Attribution）**

针对中间财务数据随步骤膨胀导致的LLM注意力衰减，将计算结果持久化于Python命名空间，LLM上下文只流转代码与推理指令，跨步骤数据直接复用；故障按根因决策：字段幻觉类错误走Traceback压缩（8行）+纠错反馈闭环，数据真空（停牌/财报未发布）直接标记不可重试避免无效循环，内置AST静态审计拦截高危系统调用；四层token预算协同控制（消息窗口、输出截断、Pandas显示、Traceback压缩）防止上下文爆表。

**10个真实故障Case中9个零干预自愈；产出示例：茅台近3年净利润CAGR=-7.01%、中国平安营收↓17.7%但净利润↑2.3%转型信号识别、工商银行vs建设银行总资产52.8万亿 vs 45.4万亿同期对齐**

**错题本驱动的在线策略对齐与DPO数据飞轮（Memory & Self-Evolvement）**

针对模型在港股格式、百分比换算等领域规范上的重复犯错，将失败轨迹自动归档、成功方案自动记录；同类意图触发时关键词检索历史教训动态注入推理上下文（上限1500字符），等效in-context few-shot纠偏，无需重训；用户偏好（格式习惯、关注指标）由后置节点跨会话持久化；同步构建DPO偏好数据飞轮，对12类错误配置差异化训练权重（字段幻觉β=0.6，网络错误β=1.0）并按任务难度Curriculum Learning分层，积累100+条偏好轨迹作为后续精调数据基础。

**研报数值100%有真实API返回值作为来源锚点，可逐条追溯；DPO轨迹格式对齐HuggingFace TRL，具备完整精调闭环基础**

---

## 技术能力

**语言与框架**：Python（熟练）/ LangGraph / LangChain / Gradio / Pandas / NumPy / Matplotlib

**大模型相关**：RAG系统设计（BM25+FAISS混合检索）/ Prompt Engineering / 结构化输出（Pydantic / with_structured_output）/ DPO数据收集与标注 / Agent规划与工具调用 / 长上下文Token预算管理

**检索与向量**：FAISS IndexFlatIP / BGE-Small-zh-v1.5 / BM25Okapi / 混合检索融合评分 / ChromaDB

**金融数据**：Tushare Pro API / 财务报表分析（income / balancesheet / fina_indicator）/ A股市场规则（停牌、港股格式、财报滞后期）

**工程能力**：多线程执行隔离（KernelManager）/ AST静态安全审计 / 状态机设计 / YAML权限矩阵热加载 / 错误分类与分级恢复

---

## 获奖 / 论文 / 其他

- [如有填写]
- **英语水平**：[CET-6 / 雅思 / 托福等]
- **GitHub**：[YOUR_GITHUB_URL]

---

## 自我评价

专注AI大模型在金融场景的工程落地，具备从系统架构设计到端到端评测的完整项目经验。将Tool Hallucination、Reasoning Drift、Context Explosion等LLM共性难题转化为可量化的工程问题：RAG Hit@3=93.1%（101样本），E2E Pass@1=90%（真实API调用）。理解DPO偏好对齐的数据层需求，已建立100+条Error-Specific标注轨迹的训练数据基础设施。对金融垂直领域的API字段非标准化、数据真空、多轮状态管理等工程陷阱有深度实战认知。

---

> **使用说明**：
> - 投**算法/RAG工程师**岗位：保留全文四个模块
> - 投**通用AI应用/产品**岗位：删除第四段DPO飞轮，在第三段末尾加业务输出案例句
> - **简历一页纸**时：每段保留第一条bullet（背景+方案）+ 最后一条加粗量化结果，其余缩减
