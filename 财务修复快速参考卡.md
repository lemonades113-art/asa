# 🎯 财务数据修复快速参考卡

## 第一轮修改状态：✅ 已完成

### 修改1：现金流字段 ✅
- **错误字段**：`c_inf_fr_operate_a`（现金流入小计）❌
- **正确字段**：`n_cashflow_act`（经营现金净额）✅
- **风险**：错误字段会高估 3-5 倍！
- **备选**：operate_cash_flow, operating_cash_flow

### 修改2：年份筛选 ✅
```python
# ✅ 正确做法
df = df[df['end_date'].str.endswith('1231')]  # 按末尾1231筛选
df = df.drop_duplicates(subset=['end_date'], keep='last')  # 去重，保留最新
```

### 修改3：财务三维分析 ✅
**必查字段**：
- `revenue` → 营收（Top Line）
- `n_income_attr_p` → 净利润（Bottom Line）
- `grossprofit_margin` → 毛利率
- `net_profit_margin` → 净利率

**关键风险识别**：
```python
is_normal = (revenue_growth > 0) AND (profit_growth > 0)  # 正常
is_warning = (revenue_growth > 0) AND (profit_growth < 0)  # 警告！
# "增收不增利" → 成本压力或竞争加剧
```

---

## 第二轮修改（待执行）

### 建议4.1：增速分析细化 🟡
- [ ] 区分"累计增速"（YoY）vs "单季增速"（QoQ）
- [ ] 优先报告YoY（稳定性更好）
- [ ] 附加QoQ（观察加速/减速趋势）

### 建议3：资产负债表优化 🟡
- [ ] 多表联动：income + cashflow + balance_sheet
- [ ] 按 end_date merge（确保数据对齐）
- [ ] 避免字段冲突和数据不全

---

## 文件位置
- **修改位置**：`multi_agent.py` L1148-L1319
- **部分**：CODER_SYSTEM_PROMPT → Tushare避坑指南

---

## 验证检查清单
- [ ] 现金流示例代码能正常运行
- [ ] 年份筛选逻辑正确（只取末尾1231）
- [ ] 财务健康度分析能识别"增收不增利"
- [ ] 输出包含完整的META元数据

---

## 系统对话示例

**用户**："分析某公司的财务健康度"

**Coder会执行**：
1. 获取3年财务数据
2. 按end_date末尾1231筛选 ✅
3. 计算营收YoY + 净利润YoY
4. 检查毛利率和净利率变化
5. 自动诊断是否"增收不增利" ✅

**Reviewer会输出**：
"某公司营收增长X%，但净利润反而下降Y%，毛利率从A%压缩到B%。这说明成本压力严重，需要关注。"

---

## 常见陷阱提醒

❌ **错误做法**：
```python
df = df[df['report_type'] == 1]  # report_type不可靠
df_cashflow['c_inf_fr_operate_a']  # 这是流入小计，会高估！
```

✅ **正确做法**：
```python
df = df[df['end_date'].str.endswith('1231')]  # 按日期筛选，更可靠
df_cashflow['n_cashflow_act']  # 真正的经营现金净额
```

---

## 效果对比

| 场景 | 修复前 | 修复后 |
|-----|-------|-------|
| 现金流数据 | ❌ 可能高估5倍 | ✅ 准确可靠 |
| 年份混乱 | ❌ 同年多条记录 | ✅ 精准去重 |
| 分析深度 | ❌ 仅看营收 | ✅ 三维评估+风险识别 |
| 报告质量 | ❌ 表面分析 | ✅ 深度见解 |

